const HomePage = () => {
    return (
        <div style={styles.container}>
            <h1>Home Page</h1>
            <p>Welcome to the home page!</p>
        </div>
    );
};

const styles = {
    container: {
        padding: '20px',
    },
};

export default HomePage;